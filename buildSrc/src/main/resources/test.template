${license}

package ${package_prefix}.advent${advent_year}.day${day}

import ${package_prefix}.advent${advent_year}.util.getInputLines
import kotlin.test.assertEquals
import kotlin.test.Test

private const val TEST_DATA = """

"""

class Day${day}Test {
    @Test
    fun testDay${day}() {
        val lines = TEST_DATA.getInputLines().map { it.toThing() }

        assertEquals(null, getPart1Answer(lines))
        assertEquals(null, getPart2Answer(lines))
    }
}